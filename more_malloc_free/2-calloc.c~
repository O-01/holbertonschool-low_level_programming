#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _calloc - allocates memory for array of n elements of sz bytes each
 * @n: array elements for which memory to be allocated
 * @sz: size of each n
 * Return: pointer to allocated memory, NULL if either input is 0 or func NULL
 */

void *_calloc(unsigned int nmemb, unsigned int size)
{
	char* 
