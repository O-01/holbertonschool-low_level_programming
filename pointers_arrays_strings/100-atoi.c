#include "main.h"

/**
 * _atoi - converts string to integer
 * @s: object string
 *
 * Return: <undecided for the moment>
 */

int _atoi(char *s)
{
	int a, p;

	if (s
		return (0);

	else
	{
		for (a = 0; s[a] != '\0'; a++)
		{
			if (a

		}
		return (*s);
	}
}
