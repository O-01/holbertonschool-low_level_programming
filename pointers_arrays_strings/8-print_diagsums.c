#include "main.h"
#include <stdio.h>

/**
 *
 *
 *
 */

void print_diagsums(int *a, int s)
{
	int x, y;

	for (x = 0; x < s; x++)
	{
		for (y = 0; y < s; y++)
		{
			printf("%d, %d\n", ());
			printf("%d, %d\n", ());
		}
		_putchar('\n');
	}
}
