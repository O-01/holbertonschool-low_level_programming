#include "main.h"

/**
 *
 *
 *
 *
 */

char *_strpbrk(char *s1, char *s2)
{
	char *x;

	while (*s1 != '\0')
	{
		x = s2;

		while (*x != '\0')
		{
			if (*s1 == *x)
				return (s1);

			x++;
		}
		s1++;
	}
	return (0);
}
