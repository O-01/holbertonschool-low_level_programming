#include "main.h"

/**
 *
 *
 *
 *
 *
 */

char *_strncpy(char *dest, char *src, int n)
{
	int x;

	if (*src != '\0')
		for (x = 0; x < n; x++)
			dest[x] = src[x];

	while (x < n)
	{
		dest[x] = '\0';
		dest++;
	}

	return (dest);
}
