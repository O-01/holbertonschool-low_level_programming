#include "main.h"

/**
 *
 *
 *
 */

void print_diagsums(int *a, int s)
{
	int x, y;

	for (x = 0; x < s; x++)
	{
		for (y = 0; y < s; y++)
		{
			_putchar(a[x]);
			_putchar(',');
			_putchar(' ');
		}
		_putchar('\n');
	}
}
