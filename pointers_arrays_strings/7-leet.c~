#include "main.h"

/**
 *
 *
 *
 */

char *leet(char *s1)
{
	int x;
	int y;
	char a1[10] = "AEOTLaeotl";
	char a2[10] = "4307143071";

	for (x = 0, y = 0; s1[x] != '\0', y < 10; x++, y++)
		if (s1[x] == a1[y])
			s1[x] = a2[y];
	s1[x] = '\0';
	return (s1);
}
