#include <stdio.h>

/**
 * main - uses printf to print exactly with proper grammar, but the outcome is a piece of art,
 *
 * Return: 0 means alles gut, printf printf'ed specified string
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,")
return (0)
}
